@echo off
echo Excel banks are now generated from source/tables JSON.
echo Run node scripts/build.mjs --all from the repository root.
echo Do not copy TXT files into data/global/excel or its base folder.
exit /b 1
